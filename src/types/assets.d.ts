/**
 * Ambient declarations for non-code imports that Metro resolves but TypeScript
 * does not know about.
 */

/** Tailwind entry stylesheet, imported for its side effect in the root layout. */
declare module '*.css';
