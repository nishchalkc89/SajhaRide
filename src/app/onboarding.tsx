/** Route: `/onboarding` — Screen 2. Thin route shell; the screen lives in features. */

import { OnboardingScreenView } from '@/features/onboarding/onboarding-screen';

export default function Onboarding() {
  return <OnboardingScreenView />;
}
