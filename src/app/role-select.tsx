/** Route: `/role-select` — passenger vs captain fork after onboarding. */

import { RoleSelectScreenView } from '@/features/auth/role-select-screen';

export default function RoleSelect() {
  return <RoleSelectScreenView />;
}
